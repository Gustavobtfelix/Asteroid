using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bala : MonoBehaviour
{/*
    public GameObject bala;
    public GameObject Posicaobala;
    private boolean atira;

    void private void Start() {
        
    }
    void Update()
    {
        if(Input.GetButtonDown("Fire1")&& atira == true)
        {
            Instantiate(bala, Posicaobala.transform.position, Posicaobala.transform.rotation);
            atira = 0;
            delayTiro();
            //projetil.velocity = transform.up * velocidadeProjetil;
        }
    }
    public IEnumerator delayTiro()
    {
        yield return = new waitsForSecunds(5);
    }*/
}